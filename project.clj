(defproject map-experiments "0.5.0-SNAPSHOT"
  :description "Persistent associative datatypes with more features than Clojure's built-in maps, including bijective maps, key-attribute-value maps, bipartite graphs, and directed graphs with arbitrary node and edge attributes."
  :dependencies [[org.clojure/clojure "1.5.1"]]
  :global-vars {*print-length* 500})
